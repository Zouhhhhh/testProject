<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cascade.mapper.FemaleHealthFormMapper">

    <select id="getFemaleHealthFormByEmpId" parameterType="long" resultType="femaleHealthForm">
        SELECT id, emp_id AS empId, heart, liver, spleen, lung, kidney, uterus, note FROM t_female_health_form
        WHERE emp_id = #{empId}
    </select>




</mapper>