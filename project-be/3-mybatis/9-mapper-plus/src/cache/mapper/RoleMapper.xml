<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cache.mapper.RoleMapper">

    <resultMap id="roleMapper" type="role">
        <id column="id" property="id"/>
        <result column="role_name" property="roleName"/>
        <result column="note" property="note"/>
        <collection property="userList" column="id" fetchType="lazy" select="cache.mapper.UserMapper.findUserByRoleId"/>
    </resultMap>

    <select id="getRole" parameterType="long" resultMap="roleMapper">
        SELECT id, role_name, note FROM t_role WHERE id = #{id}
    </select>

    <select id="findRoleByUserId" parameterType="long" resultMap="roleMapper">
        SELECT r.id, r.role_name, r.note FROM t_role r, t_user_role ur
        WHERE r.id = ur.role_id AND ur.user_id = #{userId}
    </select>

    <cache/>

</mapper>